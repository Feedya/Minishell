#include "../head.h"
//execve(path, args, envp)

void  execute_extern_command(t_line **head, t_all *all, t_line *node, int index)
{
	int nombre_de_commande;

  nombre_de_commande = count_nombre_of_commande(head);
}